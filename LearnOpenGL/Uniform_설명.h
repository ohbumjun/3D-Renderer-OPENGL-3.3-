/*
* Uniform

- cpu 에서 gpu 쪽으로 데이터를
넘겨주는 방법 중 하나이다.

- vetex attribute 와 다른 점이 있다.
1) uniform 은 global value 이다.
- 어떤 shader 이던 접근 가능
- shader 모든 단계에서 접근 가능

2) 값을 reset, update 하지 않는 이상
한번 세팅된 값 유지
*/
